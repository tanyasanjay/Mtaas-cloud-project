 

<html>

<script src="//ajax.googleapis.com/ajax/libs/angularjs/1.3.15/angular.min.js"></script>
			<link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css" rel="stylesheet">
<body>
    
    <div ng-app="MtaaS" data-ng-controller="MtaaS1">
<div class="form-group">
    <div class="form-group" ng-repeat="p in profileusername">
              <input class="form-control" ng-value="p.username" ng-init="username=p.username" type="text" name="username" ng-model= "username"> 
            </div>
	            <a input type="submit" class="btn btn-success" ng-click="signin()" role="button">Login</a>
	          </form>
</body>
<script>
	   var app=angular.module('MtaaS', []);
	 app.controller('MtaaS1', ['$scope', '$http', '$window', function ( $scope,$http,$window)
	 {
	 	$http({
			      	  method: 'GET',
			      	  url: '/profileusername'
			      	  
			        }).
			          then(function(response) {
			         //alert(response.data.result);
			         $scope.profilename = angular.fromJson(response.data.result);

			          }); 


</SCRIPT>


</html>
